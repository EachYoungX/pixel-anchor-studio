# 更新日志

## 0.4.2 - 2026-08-05

### 修复

- 修复部分特征锚定尺寸下原图网格预览显示为长方形的问题；
- 统一锚点 1×1 至 5×5 的正方形尺度、预览覆盖范围与实际采样几何；
- 打开旧项目时自动将异常的长方形锚点规范为有效正方形。

### 操作

- 顶部操作整理为导入图片、项目菜单、撤销和重做；
- PNG 导出移动到像素结果工具栏，支持 1×、2×、4×、8×及 1–16 自定义整数倍；
- PNG 导出前显示最终图片尺寸和文件名。

### 稳定性

- 删除采样请求中的第二套回退几何，预览、缓存和 Worker 共用同一网格参数；
- 增加锚点尺度、网格预览边界、项目锚点迁移和导出入口测试；
- 保持中文界面，并集中拼豆文档文字以便后续按需国际化。

## 0.4.1 - 2026-08-05

### 操作

- 改进原图快速拖动、缩放和平移，指针移出预览区域后仍可连续操作；
- 大图片使用轻量显示预览，减少裁剪框拖动时的闪现和卡顿；
- 减少框体拖动和连续画笔中的重复刷新。

### 关于工具

- 补充项目声明、隐私说明、使用责任和完整 MIT License；
- 最近更新自动读取项目更新日志，避免版本信息不同步；
- 整理 GitHub 项目入口并增强底部主按钮。

### 稳定性

- 完善画布 Pointer Capture、预览资源释放和帧合并处理；
- 增加原图代理预览、绘制调度和更新日志解析测试；
- 保持现有像素转换结果、项目文件兼容性和后台处理路径不变。

## 0.4.0 - 2026-08-04

### 操作

- 统一原图和像素结果的缩放、平移与恢复视图；
- 普通滚轮在两个画布中都用于中央页面滚动；
- 像素结果改用逻辑分辨率绘制，连续画笔响应更稳定。

### 处理与稳定性

- 优化大图片后台处理、采样和重复生成；
- 改进项目保存、项目迁移、Worker 超时和资源释放；
- 保持默认像素结果与既有采样行为兼容。

## 0.3.3 - 2026-08-04

### 界面

- 扩大网格偏移数值输入框，负数和小数值更容易完整查看；
- 原图与结果之间取消滚动吸附，并在原图页面下方持续露出像素结果入口；
- 新导入图片的默认像素长边调整为 32，同时保留已保存项目的原有尺寸。
- 网格相位偏移输入框与其他数值控件统一宽度和样式。
- 原图页进一步缩短，中央视口中可更早看到像素结果标题。
- 输出尺寸信息移动到像素结果页，和结果内容保持一致。
- 伪像素对齐尺度上限提高到 128，适应更大的原图。

### 预览

- 改进拼豆文档的初始大小和居中显示；
- 拼豆文档改为固定中心缩放，放大后仍可通过普通滚动返回页面顶部；
- 原图与像素结果统一将视图操作放在工具栏右侧；
- 像素结果新增“显示网格”开关。
- 关于工具页签保持固定位置，颜色图例列间距更清晰。
- 修复像素结果双击时误选右侧颜色文字的问题。

### 整理

- 页面图标统一使用 favicon；
- 完善关于工具窗口；
- 拼豆导出入口统一保留在拼豆图导出工作区。

## 0.3.2 - 2026-08-04

### 界面与视图

- 原图和像素结果工作页在中央区域完整铺开，并保持独立滚动；
- 原图、像素结果和拼豆预览统一支持适应窗口、缩放、平移、双击恢复和窗口尺寸变化；
- 放大工作区标签、侧边栏文字和关于工具窗口，关于窗口增加使用方法、更新日志和项目许可页签；
- 顶部操作栏移除重复的拼豆导出按钮，拼豆导出集中在对应工作区；
- 统一使用同一份应用图标资源，并补充键盘焦点恢复和 Escape 关闭支持。

### 拼豆文档

- PDF 分页、SVG 总览和在线预览使用一致的页面、文字、颜色和网格样式；
- 修正 SVG 与 PDF 的字号单位换算，改善不同查看器中的文字比例；
- 颜色图例固定显示色块、编号、HEX 和用量字段，超过 64 种颜色时自动追加图例页；
- PDF 预览可直接查看完整分页文档，避免预览与实际导出结果不一致。

## 0.3.1 - 2026-08-04

### 界面与操作

- 原图和像素结果改为两个完整工作页，可在中央区域上下滚动查看；
- 像素编辑和拼豆导出的颜色统计统一显示在右侧；
- 原图、像素结果和拼豆预览统一支持缩放、平移和恢复视图；
- 移除重复的缩放数字和像素结果内部撤销入口；
- 补充框体吸附、图像范围和特征锚定的使用说明；
- 更新产品标题、图标和关于工具入口。

### 拼豆图

- 新增 PDF 分页和 SVG 总览两种文档预览；
- 预览中的页面、行列号、页码和颜色图例与实际导出使用同一套布局；
- 拼豆页面颜色列表改为右侧只读统计栏，避免预览阶段误改像素结果；
- PDF 图例标题改为“颜色与用量”。

### 修复

- 框体靠近画布上边界时不再将标签移入框内；
- 修复中央工作页固定高度压缩，改善原图与像素结果的完整审视空间；
- 统一工作区中颜色栏、历史记录和像素工具栏的职责。

### 文档

- README 改为面向使用者介绍功能和操作流程；
- 更新日志仅保留用户可感知的版本变化，移除内部开发阶段和实现细节。

## 0.3.0 - 2026-08-04

- 增加高 DPI 原图 Canvas、离散像素缩放、全局撤销/重做和带操作名称的历史记录；
- 使用统一正方形网格处理原图显示、采样和裁剪边界；
- 拼豆工作区支持 32 × 32 默认分页、A4 横向 PDF 和 64 色图例页；
- 项目文件升级为 v3，并兼容旧项目迁移；
- 图片预览使用 Object URL，采样过程改用扁平缓冲；
- 增加大图采样、网格几何、项目迁移和导出布局测试。

## 0.2.0 - 2026-08-04

- 改进裁剪预设、原图与像素结果视图、连续画笔和颜色处理；
- 增加 HSL 色环排序、相近色合并、目标网格吸附和拼豆网格预览；
- 增加 SVG、PDF、CSV 导出以及 Worker 缓存和处理结果缓存；
- 优化中位数与主色块采样，并补充颜色处理说明。

## 0.1.0 - 2026-08-04

- 建立本地图片像素化、裁剪、特征锚定、伪像素对齐和基础颜色处理流程；
- 支持像素编辑、项目保存、PNG、拼豆 SVG、PDF 和颜色 CSV 导出；
- 图像处理在浏览器本地完成，不上传图片。
